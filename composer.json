{
    "name": "shadabrcspl/admin-approval-guard",
    "description": "Enforces mandatory administrator approval before any user account can receive WordPress administrator privileges.",
    "license": "GPL-2.0-only",
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "phpunit/phpunit": "^9.5"
    },
    "autoload": {
        "classmap": ["plugin-security-check.php"]
    },
    "scripts": {
        "test": "php phpunit.phar --colors=always --testdox"
    }
}
